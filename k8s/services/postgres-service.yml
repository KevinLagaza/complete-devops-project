apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: icgroup
  labels:
    app: postgres
    env: prod
spec:
  type: ClusterIP
  selector:
    app: postgres
    env: prod
  ports:
    - port: 5432
      targetPort: 5432