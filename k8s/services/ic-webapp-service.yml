apiVersion: v1
kind: Service
metadata:
  name: ic-webapp-service
  namespace: icgroup
  labels:
    app: ic-webapp
    env: prod
spec:
  type: ClusterIP
  selector:
    app: ic-webapp
  ports:
    - port: 8080
      targetPort: 8080