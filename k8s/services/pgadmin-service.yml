apiVersion: v1
kind: Service
metadata:
  name: pgadmin-service
  namespace: icgroup
  labels:
    app: pgadmin
    env: prod
spec:
  type: NodePort
  selector:
    app: pgadmin
    env: prod
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
      nodePort: 30050